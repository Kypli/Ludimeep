<form
	method="post"
	action="{{ path('discussion_delete', {'id': d.id}) }}"
	onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette discussion ? Cette action est irréversible !');
">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ d.id) }}">
	<button class="btn {{ btn is defined and btn == false ? 'pt-0' : 'btn-danger' }}">
		{% if btn is defined and btn == false %}
			<i class="fa fa-times red" title="Supprimer la discussion"></i>
		{% else %}
			Supprimer
		{% endif %}
	</button>
</form>
