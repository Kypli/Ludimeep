{{ form_start(form) }}

    <div class="row center mt-4">

        {# JEUX USER #}
        <div class="offset-2 col-3 mb-4 alignRight">
            {{ form_label(form.gameOwner) }}
        </div>
        <div class="col-4 mb-4">
            {{ form_widget(form.gameOwner) }}
        </div>

        {# JEUX PRESENT #}
        <div class="offset-2 col-3 mb-4 alignRight">
            {{ form_label(form.gamePresent) }}
        </div>
        <div class="col-4 mb-4">
            {{ form_widget(form.gamePresent) }}
        </div>

        {# JEUX ADHERANT #}
        <div class="offset-2 col-3 mb-4 alignRight">
            {{ form_label(form.gameAdherant) }}
        </div>
        <div class="col-4 mb-4">
            {{ form_widget(form.gameAdherant) }}
        </div>
    </div>

    <button class="btn">{{ button_label|default('Sauvegarder') }}</button>

{{ form_end(form) }}