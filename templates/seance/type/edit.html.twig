{% extends 'base.html.twig' %}

{% block title %}Modifier une séance{% endblock %}

{% block body %}
	<div class="container">
		<div class='row mb-3'>

			{# TITRE #}
			<h2 class="radius-long offset-4 col-4">Modifier une séance</h2>

			{# TABLEAU LISTE #}
			<section class='col-12 mb-3'>

				{# RETOUR #}
				<div class="col-3">
					<a href="{{ path('seance_type') }}" class="btn btn-info">Retour</a>
				</div>

				{{ include('seance/type/_form.html.twig', {'button_label': 'Modifier'}) }}
			</section>
		</div>
	</div>
{% endblock %}