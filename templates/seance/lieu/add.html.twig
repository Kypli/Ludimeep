{% extends 'base.html.twig' %}

{% block title %}Ajouter un lieu de séance{% endblock %}

{% block body %}
	<div class="container">
		<div class='row mb-3'>

			{# TITRE #}
			<h2 class="radius-long offset-4 col-4">Ajouter un lieu de séance</h2>

			{# TABLEAU LISTE #}
			<section class='col-12 mb-3'>

				{# RETOUR #}
				<div class="col-3">
					<a href="{{ path('seance_lieu') }}" class="btn btn-info">Retour</a>
				</div>

				{{ include('seance/lieu/_form.html.twig') }}
			</section>
		</div>
	</div>
{% endblock %}
